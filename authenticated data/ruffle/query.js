SELECT fields with aggregation
FROM table
WHERE expression
GROUP BY group
ORDER BY orderby ASC/DESC
LIMIT limit



SELECT fields
FROM table
WHERE expression

SELECT fields
FROM table
WHERE expression
LIMIT limit

SELECT fields
FROM table
WHERE expression
ORDER BY orderby ASC/DESC
LIMIT limit

SELECT fields with aggregation
FROM table
WHERE expression
GROUP BY group
ORDER BY orderby ASC/DESC
LIMIT limit
